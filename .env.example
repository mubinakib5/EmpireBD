# Gmail Configuration for Order Notifications
# You need to set these environment variables in your .env.local file

# Gmail account that will send the emails
GMAIL_USER=your-email@gmail.com

# Gmail App Password (NOT your regular Gmail password)
# Generate this from: Google Account > Security > 2-Step Verification > App passwords
GMAIL_APP_PASSWORD=your-16-character-app-password

# Admin email to receive order notifications (optional, defaults to GMAIL_USER)
ADMIN_EMAIL=admin@empirebd.com

# Your website URL (used for email links and API calls)
NEXTAUTH_URL=http://localhost:3001

# Viewer tracking has been removed. Configure refined fake count for urgency.

# Fake viewer baseline range (starting value)
NEXT_PUBLIC_VIEWER_FAKE_MIN_START=3
NEXT_PUBLIC_VIEWER_FAKE_MAX_START=8

# Fake viewer ceiling (upper target) and upward bias
NEXT_PUBLIC_VIEWER_FAKE_MAX_TARGET=16
NEXT_PUBLIC_VIEWER_FAKE_UP_BIAS=0.7

# Viewer count jitter (UI-only): randomized every 20–40 seconds
NEXT_PUBLIC_VIEWER_COUNT_JITTER_ENABLED=true
NEXT_PUBLIC_VIEWER_COUNT_JITTER_MIN_MS=20000
NEXT_PUBLIC_VIEWER_COUNT_JITTER_MAX_MS=40000

# Optional: Protect automated cleanup endpoint
# Create a random token and set it both in env and your cron job
CRON_SECRET_TOKEN=your-cron-secret-token

# Existing environment variables (keep your current values)
# SANITY_PROJECT_ID=your-project-id
# SANITY_DATASET=production
# SANITY_API_TOKEN=your-api-token
# NEXTAUTH_SECRET=your-nextauth-secret